/**
 * @lamalibre/lamaste-agentd — Agent panel HTTP daemon.
 *
 * Exports the server creation function for programmatic use
 * (e.g., from panel-service.js entry points or test harnesses).
 */

export { startPanelServer } from './server.js';
